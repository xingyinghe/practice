身份证号码验证：
    1，布局
       一个div里放着一个文本框，用于输入身份证号码
       另一个div用于返回结果

    2，js部分
       1. 获取文本框 txt
       2. 获取按钮 btn
       3. 获取结果div   result
       4. 给按钮绑定点击事件
         系数和校验值是固定的
            a，获取用户输入的内容
                toUpperCase() 把英文字符转换为大字母
                toLowerCase() 把英文字符转换为小字母
            b，检查输入的内容长度是否为18，如果不是18位提示错误信息，并结束运行
            c，通过循环获取输入字符串前17位的内容
                 使用当前位置的系数*当前位置的身份证数字
                 并对结果进行累加
            d，使用累加和11求余
            e，根据余数获取对应的校验值
            f，使用校验值与第18位比较，结果一致身份证号码有效