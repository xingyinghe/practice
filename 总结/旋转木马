旋转木马：
    1、布局：
        大的盒子里包含轮播图片的列表，和一个包含左右键的div
    2、js
        1、获取包含图片的li叫arr，还有左右按键
        2、用json给每张图片定好位置
        3、定义一个数组var liArr = [];
        4、for循环，i<arr.length，使图片向左移动  liArr.push(arr[i]);
        5、给左右键分别设置点击事件：
                a，前一个按钮（prev）：
                    先把第一个元素移出，然后在最后面添加第一个元素，调用move函数
                b，下一个按钮（next）：
                    把最后一个元素移出，在最前面添加最后一个元素，调用move函数
        6、设置move函数，设置图片移动距离

























